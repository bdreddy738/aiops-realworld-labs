# Pandas library used for data processing and CSV file handling
# Used in fetch_metrics.py to create DataFrames
# Used in train_model.py to read cpu_metrics.csv
pandas==2.0.3

# Scikit-Learn Machine Learning library
# Provides Isolation Forest algorithm
# Used for anomaly detection model training
scikit-learn==1.3.0

# Used to save and load trained ML models
# Saves aiops_model.joblib
# Loads aiops_model.joblib during real-time monitoring
joblib==1.3.2

# Used for REST API communication
# Fetches metrics from Prometheus
# Sends alerts to Slack Webhook
requests==2.31.0

# Used to read configuration from config.ini
# Loads Prometheus URL, PromQL query, and Slack Webhook URL
configparser==6.0.1